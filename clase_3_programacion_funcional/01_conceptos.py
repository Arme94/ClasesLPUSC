#uso de funciones de orden superior: map, filter, reduce
from functools import reduce 
'''
lista = list(range(0,1001,1))
list_pares = list(filter(lambda x: x%2==0, lista))

print(list_pares)
suma = reduce(lambda x,y: x+y, list_pares)

print(suma)
'''
